# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: deploy
on:
  push:
    branches:
      - main
  workflow_dispatch: {}
jobs:
  synth:
    name: Synth CDK application
    needs: []
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - run: echo "GITHUB_TOKEN=${{ secrets.GITHUB_TOKEN }}" >> $GITHUB_ENV
      - run: npx projen install:ci
      - run: npx projen build
      - name: Upload Artifact
        uses: actions/upload-artifact@v4.3.6
        with:
          name: cloud-assembly
          path: cdk.out/
  assetUpload:
    name: Publish assets to AWS
    needs: synth
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
      packages: write
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: git config --global user.name "github-actions" && git config --global user.email "github-actions@github.com"
      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: cloud-assembly
          path: cdk.out/
      - run: echo "GITHUB_TOKEN=${{ secrets.GITHUB_TOKEN }}" >> $GITHUB_ENV
      - run: npx projen install:ci
      - name: AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: cdk-hnb659fds-file-publishing-role-352770552266-eu-central-1
          role-session-name: GitHubAction
          aws-region: us-east-1
      - run: npx projen publish:assets:dev
      - name: AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: cdk-hnb659fds-file-publishing-role-505825668341-eu-central-1
          role-session-name: GitHubAction
          aws-region: us-east-1
      - run: npx projen publish:assets:prod
      - run: npx projen bump
      - run: npx projen release:push-assembly
  deploy-dev:
    name: Deploy stage dev to AWS
    needs: assetUpload
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
      id-token: write
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: cloud-assembly
          path: cdk.out/
      - run: echo "GITHUB_TOKEN=${{ secrets.GITHUB_TOKEN }}" >> $GITHUB_ENV
      - run: npx projen install:ci
      - name: AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: cdk-hnb659fds-deploy-role-352770552266-eu-central-1
          role-session-name: GitHubAction
          aws-region: eu-central-1
      - run: npx projen deploy:dev
      - name: Upload Artifact
        uses: actions/upload-artifact@v4.3.6
        with:
          name: cdk-outputs-dev
          path: cdk-outputs-dev.json
